<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{% block title %} Platforme {% endblock %}</title>
    {% block stylesheets %}
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <style>
            body {
                font-family: sans-serif;
                padding-top: 39px;
                font-size: 15px;
            }
        </style>
    {% endblock %}
</head>
<body>
<div class="container">
    <div class="jumbotron">
        <h1>Ma plate-forme d'annonces</h1>
        <p>
            ce projet est propulsé par symfony,et construit grâce à OpenClassroom,SensioLabs.
        </p>
        <p>
            <a href="#" class="btn btn-lg btn-primary">Partciper au MOOC</a>
        </p>
    </div>

    <div class="row">
        <div id="menu" class="col-md-3">
            <h3>Les annonces</h3>
            <ul class="nav nav-pills nav-stacked">
                <li><a href="{{ path('app_platform_home') }}">Accueil</a></li>
                <li><a href="{{ path('app_platform_add') }}">Ajouter une annonce</a></li>
            </ul>
            <h4>Derniéres annonces</h4>
            {{ render(controller('AppBundle:Advert:menu',{ 'limit' : 3})) }}
        </div>
        <div id="content" class="col-md-9">

            {# MESSAGE FLASH #}
            {% for message in app.flashes('notice') %}
                <div class="alert alert-success">
                    {{ message }}
                </div>
            {% endfor %}

            {# BLOC BODY #}
            {% block body %}
            {% endblock %}
            
        </div>
    </div>
</div>

<hr>

<footer style="margin-bottom: 10px">
    {{ webmaster }}
</footer>
{% block javascripts %}
{% endblock %}
</body>
</html>