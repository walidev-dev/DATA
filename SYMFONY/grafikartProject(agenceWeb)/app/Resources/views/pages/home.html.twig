{% extends 'base.html.twig' %}
{% block title 'Agence' %}
{% block body %}
    <div class="jumbotron text-center">
        <h1>Ma Super Agence</h1>
        <p>
            Lorem ipsum dolor sit amet, consectetur adipisicing elit. Ab, amet atque autem
            consectetur error est et eveniet libero maiores nam nobis omnis quam quasi quo, quod rem sequi tenetur.
            Libero!
        </p>
    </div>
    <div class="container">
        <h2>Les Derniers biens</h2>
        <div class="row flex">
            {% for property in properties %}
                <div class="col-md-3">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <a href="{{ path('property_show',{'id':property.id,'slug':property.slug}) }}">
                                    {{ property.title }}
                                </a>
                            </h5>
                            <p class="card-text">
                                {{ property.city }}({{ property.postalCode }})
                            </p>
                            <div class="text-primary" style="font-size: 2rem;font-weight: bold">
                                {{ property.formattedPrice }} €
                            </div>

                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
